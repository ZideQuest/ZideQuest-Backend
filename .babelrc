{
    "presets": [
        "@babel/preset-env",
        [
            "babel-preset-vite",
            {
                "env": true, // defaults to true
                "glob": false // defaults to true
            }
        ]
    ]
}
